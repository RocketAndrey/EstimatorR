@page
@model Estimator.Pages.ElementKey.DeleteModel

@{
    ViewData["Title"] = "Удаление ключа";
}


<h3>AВы уверены что хотите удалить этот ключ?</h3>
<div>
   
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ElementKey.ElementType.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ElementKey.ElementType.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ElementKey.Key)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ElementKey.Key)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="ElementKey.ElementKeyID" />
        <input type="submit" value="Удалить" class="btn btn-outline-danger" /> |
        <a asp-page="./Index" class="btn btn-outline-primary" asp-route-id="@Model.ElementKey.ElementTypeID">Список</a>
    </form>
</div>
